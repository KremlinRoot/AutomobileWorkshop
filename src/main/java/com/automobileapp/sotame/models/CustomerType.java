package com.automobileapp.sotame.models;

/**
 * Defines the type of customer (Private, Business, Individual, Other)
 */
public enum CustomerType{
    PRIVATE,
    BUSINESS,
    INDIVIDUAL,
    OTHER
}
